@model IEnumerable<MvcCoreApiSeriesPersonajes.Models.Serie>

@{
    ViewData["Title"] = "Index";
}

<h4>Series</h4>

<p>
    <a asp-action="Create">Crear nueva serie</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.IdSerie)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Nombre)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Imagen)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Puntuacion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Anyo)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.IdSerie)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Nombre)
                </td>
                <td>
                    <img src="@item.Imagen" style="width: 250px; height: 250px;" />
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Puntuacion)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Anyo)
                </td>
                <td>
                    @Html.ActionLink("Update", "Update", new {  id=item.IdSerie  }, new { @class = "btn btn-info" }) |
                    @Html.ActionLink("Details", "Details", new {  id=item.IdSerie  }, new { @class = "btn btn-warning" }) |
                    @Html.ActionLink("Delete", "Delete", new {  id=item.IdSerie  }, new { @class = "btn btn-danger" })
                </td>
            </tr>
        }
    </tbody>
</table>
